# Todo Shared Domain

This folder contains the browser-safe Todo domain model and pure helper functions.

- Keep Todo rules and invariants here.
- Do not import Svelte, SvelteKit, or app-specific UI code here.
- Frontend code should consume this module through `domain/shared`.
